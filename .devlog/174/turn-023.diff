diff --git a/.github/pr-notification/discord-pr-notification.ts b/.github/pr-notification/discord-pr-notification.ts
index 0208c3fa616d59568d56e3fbc8c5594256061688..4e76bf2be169e12408f7ab25378f9b2742baf096 100644
--- a/.github/pr-notification/discord-pr-notification.ts
+++ b/.github/pr-notification/discord-pr-notification.ts
@@ -1,50 +1,3 @@
-type Label = {
-  name?: string;
-};
-
-type EventDescription = {
-  icon: string;
-  text: string;
-};
-
-type DiscordField = {
-  name: string;
-  value: string;
-  inline: boolean;
-};
-
-type DiscordPayload = {
-  embeds: Array<{
-    title: string;
-    url: string;
-    description: string;
-    fields: DiscordField[];
-  }>;
-};
-
-export type Member = {
-  github: string;
-  nickname: string;
-  discordUserId?: string;
-};
-
-export type NotificationInput = {
-  action: string;
-  merged: boolean;
-  number: string;
-  title: string;
-  url: string;
-  head: string;
-  base: string;
-  actor: string;
-  author: string;
-  commits: string;
-  files: string;
-  additions: string;
-  deletions: string;
-  labels: Label[];
-};
-
 const WEBHOOK_CHANNELS: Record<string, string> = {
   "release-be": "BE",
   "release-fe": "FE",
@@ -91,7 +44,6 @@ export function loadMembers(value: string | undefined): Member[] {
   if (!parsed || typeof parsed !== "object" || !("members" in parsed) || !Array.isArray(parsed.members)) {
     throw new Error("PR_NOTIFICATION_MEMBERS_JSON must contain a members array");
   }
-
   if (!parsed.members.every(isMember)) {
     throw new Error("every member must have string github and nickname properties");
   }
@@ -220,3 +172,50 @@ if (process.argv[1] && import.meta.url === new URL(`file://${process.argv[1]}`).
     process.exitCode = 1;
   });
 }
+
+type Label = {
+  name?: string;
+};
+
+type EventDescription = {
+  icon: string;
+  text: string;
+};
+
+type DiscordField = {
+  name: string;
+  value: string;
+  inline: boolean;
+};
+
+type DiscordPayload = {
+  embeds: Array<{
+    title: string;
+    url: string;
+    description: string;
+    fields: DiscordField[];
+  }>;
+};
+
+export type Member = {
+  github: string;
+  nickname: string;
+  discordUserId?: string;
+};
+
+export type NotificationInput = {
+  action: string;
+  merged: boolean;
+  number: string;
+  title: string;
+  url: string;
+  head: string;
+  base: string;
+  actor: string;
+  author: string;
+  commits: string;
+  files: string;
+  additions: string;
+  deletions: string;
+  labels: Label[];
+};
