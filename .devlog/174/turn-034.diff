diff --git a/.github/pr-notification/discord-pr-notification.test.ts b/.github/pr-notification/discord-pr-notification.test.ts
index 7d8b675641707f0b785aed262f55bfca859dbb07..91bdfe4fa2ab4814e7b3909bb122ebb551eda790 100644
--- a/.github/pr-notification/discord-pr-notification.test.ts
+++ b/.github/pr-notification/discord-pr-notification.test.ts
@@ -1,7 +1,7 @@
 import assert from "node:assert/strict";
 import test from "node:test";
 
-import { loadMembers } from "./member-configuration.ts";
+import { loadMembers } from "./member-configuration-parser.ts";
 import { buildPayload, channelForBaseBranch } from "./discord-pr-notification.ts";
 
 const input = { action: "opened", merged: false, number: "174", title: "코드 리뷰를 빠르게 하기", url: "https://github.com/woowacourse-teams/2026-bibbidi/pull/174", head: "feature/174", base: "release-fe", actor: "kdongsu5509", author: "boyekim", labels: [{ name: "type: feature" }, { name: "area: fe" }] };
@@ -12,30 +12,30 @@ const membersJson = JSON.stringify({ members: [
   { github: "JohnPrk", nickname: "티뉴", discordUserId: "" },
 ] });
 
-test("routes supported release branches to their existing webhook channels", () => {
-  assert.equal(channelForBaseBranch("release-be"), "BE");
-  assert.equal(channelForBaseBranch("release-fe"), "FE");
-  assert.equal(channelForBaseBranch("release-app"), "APP");
-  assert.equal(channelForBaseBranch("main"), undefined);
+test("릴리스 브랜치를 기존 Discord 웹훅 채널로 연결한다", () => {
+  assert.equal(channelForBaseBranch("release-be"), "BE", "BE 릴리스 브랜치는 BE 채널을 사용해야 합니다.");
+  assert.equal(channelForBaseBranch("release-fe"), "FE", "FE 릴리스 브랜치는 FE 채널을 사용해야 합니다.");
+  assert.equal(channelForBaseBranch("release-app"), "APP", "APP 릴리스 브랜치는 APP 채널을 사용해야 합니다.");
+  assert.equal(channelForBaseBranch("main"), undefined, "main 브랜치에는 알림 채널이 없어야 합니다.");
 });
 
-test("loads GitHub login and nickname mappings from the JSON configuration", () => {
+test("JSON 설정에서 GitHub 로그인과 닉네임을 읽는다", () => {
   const members = loadMembers(membersJson);
   assert.deepEqual(members.map(({ github, nickname }) => ({ github, nickname })), [
     { github: "kdongsu5509", nickname: "라티" },
     { github: "byunghyunkim0", nickname: "바드" },
     { github: "boyekim", nickname: "보예" },
     { github: "JohnPrk", nickname: "티뉴" },
-  ]);
-  assert(members.every((member) => typeof member.discordUserId === "string"));
+  ], "구성원 설정의 GitHub 로그인과 닉네임이 일치해야 합니다.");
+  assert(members.every((member) => typeof member.discordUserId === "string"), "Discord 사용자 ID는 문자열이어야 합니다.");
 });
 
-test("builds the existing opened PR notification payload", () => {
-  assert.deepEqual(buildPayload(input, loadMembers(membersJson)), { embeds: [{ title: "🆕 라티님이 PR을 생성했습니다", url: input.url, description: "#174 코드 리뷰를 빠르게 하기", fields: [{ name: "작성자", value: "`보예 (@boyekim)`", inline: true }, { name: "브랜치", value: "`feature/174 → release-fe`", inline: true }, { name: "라벨", value: "type: feature, area: fe", inline: false }] }] });
+test("PR 생성 알림 페이로드를 만든다", () => {
+  assert.deepEqual(buildPayload(input, loadMembers(membersJson)), { embeds: [{ title: "🆕 라티님이 PR을 생성했습니다", url: input.url, description: "#174 코드 리뷰를 빠르게 하기", fields: [{ name: "작성자", value: "`보예 (@boyekim)`", inline: true }, { name: "브랜치", value: "`feature/174 → release-fe`", inline: true }, { name: "라벨", value: "type: feature, area: fe", inline: false }] }] }, "PR 생성 알림 페이로드가 예상 형식과 일치해야 합니다.");
 });
 
-test("uses merge and close messages according to the merged flag", () => {
+test("병합 여부에 따라 병합 또는 종료 메시지를 사용한다", () => {
   const members = loadMembers(membersJson);
-  assert.match(buildPayload({ ...input, action: "closed", merged: true }, members).embeds[0].title, /머지했습니다/);
-  assert.match(buildPayload({ ...input, action: "closed", merged: false }, members).embeds[0].title, /닫았습니다/);
+  assert.match(buildPayload({ ...input, action: "closed", merged: true }, members).embeds[0].title, /머지했습니다/, "병합된 PR에는 병합 메시지를 표시해야 합니다.");
+  assert.match(buildPayload({ ...input, action: "closed", merged: false }, members).embeds[0].title, /닫았습니다/, "닫힌 PR에는 종료 메시지를 표시해야 합니다.");
 });
diff --git a/.github/pr-notification/discord-pr-notification.ts b/.github/pr-notification/discord-pr-notification.ts
index 40b73b6ba7131e92e054d894c7a78d8d229eb7c0..7f6b8753cb30e723f23067ced2a42ceb255ed8e6 100644
--- a/.github/pr-notification/discord-pr-notification.ts
+++ b/.github/pr-notification/discord-pr-notification.ts
@@ -1,4 +1,4 @@
-import { loadMembers, type Member } from "./member-configuration.ts";
+import { loadMembers, type Member } from "./member-configuration-parser.ts";
 
 const WEBHOOK_CHANNELS: Record<string, string> = {
   "release-be": "BE",
@@ -112,7 +112,7 @@ export async function sendPayload(webhook: string, payload: DiscordPayload): Pro
       if (response.ok) return;
       lastError = new Error(`Discord 웹훅 요청에 실패했습니다. (상태 코드: ${response.status})`);
     } catch (error) {
-      lastError = error instanceof Error ? error : new Error(String(error));
+      lastError = new Error("Discord 웹훅 요청 중 네트워크 오류가 발생했습니다.");
     }
 
     if (attempt < RETRY_COUNT) {
@@ -120,7 +120,7 @@ export async function sendPayload(webhook: string, payload: DiscordPayload): Pro
     }
   }
 
-  throw lastError;
+  throw lastError ?? new Error("Discord 웹훅 요청에 실패했습니다.");
 }
 
 export async function main(environment = process.env): Promise<void> {
diff --git a/.github/pr-notification/member-configuration.ts b/.github/pr-notification/member-configuration-parser.ts
similarity index 100%
rename from .github/pr-notification/member-configuration.ts
rename to .github/pr-notification/member-configuration-parser.ts
diff --git a/.github/pr-notification/reviewer-assignment.test.ts b/.github/pr-notification/reviewer-assignment.test.ts
index 9a8bb7d5545b004538522aba903d3184c9cfdd5d..c702356b9ab2ec4c6e9d2193bf67745d91290c13 100644
--- a/.github/pr-notification/reviewer-assignment.test.ts
+++ b/.github/pr-notification/reviewer-assignment.test.ts
@@ -10,12 +10,12 @@ const candidates = [
   { github: "JohnPrk" },
 ];
 
-test("assigns reviewers by PR-number round robin while skipping the author and excluded members", () => {
-  assert.equal(selectReviewer({ candidates, author: "kdongsu5509", pullNumber: 1, existingReviewers: [] }), "boyekim");
-  assert.equal(selectReviewer({ candidates, author: "boyekim", pullNumber: 3, existingReviewers: [] }), "JohnPrk");
-  assert.equal(selectReviewer({ candidates, author: "JohnPrk", pullNumber: 4, existingReviewers: [] }), "kdongsu5509");
+test("PR 번호 순환으로 작성자와 제외 구성원을 건너뛰어 리뷰어를 배정한다", () => {
+  assert.equal(selectReviewer({ candidates, author: "kdongsu5509", pullNumber: 1, existingReviewers: [] }), "boyekim", "작성자는 건너뛰고 다음 후보를 배정해야 합니다.");
+  assert.equal(selectReviewer({ candidates, author: "boyekim", pullNumber: 3, existingReviewers: [] }), "JohnPrk", "제외된 후보는 배정하지 않아야 합니다.");
+  assert.equal(selectReviewer({ candidates, author: "JohnPrk", pullNumber: 4, existingReviewers: [] }), "kdongsu5509", "후보 목록 끝에서는 처음 후보로 순환해야 합니다.");
 });
 
-test("does not replace a reviewer already selected for the PR", () => {
-  assert.equal(selectReviewer({ candidates, author: "kdongsu5509", pullNumber: 1, existingReviewers: ["boyekim"] }), undefined);
+test("이미 지정된 리뷰어를 바꾸지 않는다", () => {
+  assert.equal(selectReviewer({ candidates, author: "kdongsu5509", pullNumber: 1, existingReviewers: ["boyekim"] }), undefined, "기존 리뷰어가 있으면 자동 배정하지 않아야 합니다.");
 });
diff --git a/.github/pr-notification/reviewer-assignment.ts b/.github/pr-notification/reviewer-assignment.ts
index d86bfadb930b6cf754af718be163802c97833d5f..0375ac416f072abb1d468578d12e5b9bdbc49e91 100644
--- a/.github/pr-notification/reviewer-assignment.ts
+++ b/.github/pr-notification/reviewer-assignment.ts
@@ -99,4 +99,4 @@ if (process.argv[1] && import.meta.url === new URL(`file://${process.argv[1]}`).
     process.exitCode = 1;
   });
 }
-import { loadMembers } from "./member-configuration.ts";
+import { loadMembers } from "./member-configuration-parser.ts";
