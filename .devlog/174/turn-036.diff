diff --git a/.github/pr-notification/discord-pr-notification.test.ts b/.github/pr-notification/discord-pr-notification.test.ts
index 91bdfe4fa2ab4814e7b3909bb122ebb551eda790..17fcb87537766f54318a75015fc49f525645a22f 100644
--- a/.github/pr-notification/discord-pr-notification.test.ts
+++ b/.github/pr-notification/discord-pr-notification.test.ts
@@ -4,7 +4,7 @@ import test from "node:test";
 import { loadMembers } from "./member-configuration-parser.ts";
 import { buildPayload, channelForBaseBranch } from "./discord-pr-notification.ts";
 
-const input = { action: "opened", merged: false, number: "174", title: "코드 리뷰를 빠르게 하기", url: "https://github.com/woowacourse-teams/2026-bibbidi/pull/174", head: "feature/174", base: "release-fe", actor: "kdongsu5509", author: "boyekim", labels: [{ name: "type: feature" }, { name: "area: fe" }] };
+const input = { eventName: "pull_request", action: "opened", merged: false, number: "174", title: "코드 리뷰를 빠르게 하기", url: "https://github.com/woowacourse-teams/2026-bibbidi/pull/174", head: "feature/174", base: "release-fe", actor: "kdongsu5509", author: "boyekim", labels: [{ name: "type: feature" }, { name: "area: fe" }] };
 const membersJson = JSON.stringify({ members: [
   { github: "kdongsu5509", nickname: "라티", discordUserId: "" },
   { github: "byunghyunkim0", nickname: "바드", discordUserId: "" },
@@ -31,11 +31,16 @@ test("JSON 설정에서 GitHub 로그인과 닉네임을 읽는다", () => {
 });
 
 test("PR 생성 알림 페이로드를 만든다", () => {
-  assert.deepEqual(buildPayload(input, loadMembers(membersJson)), { embeds: [{ title: "🆕 라티님이 PR을 생성했습니다", url: input.url, description: "#174 코드 리뷰를 빠르게 하기", fields: [{ name: "작성자", value: "`보예 (@boyekim)`", inline: true }, { name: "브랜치", value: "`feature/174 → release-fe`", inline: true }, { name: "라벨", value: "type: feature, area: fe", inline: false }] }] }, "PR 생성 알림 페이로드가 예상 형식과 일치해야 합니다.");
+  assert.deepEqual(buildPayload(input, loadMembers(membersJson)), { content: "라티님이 [#174 : 코드 리뷰를 빠르게 하기](https://github.com/woowacourse-teams/2026-bibbidi/pull/174)을 생성했습니다.\ntype: feature" }, "PR 생성 알림 페이로드가 예상 형식과 일치해야 합니다.");
 });
 
 test("병합 여부에 따라 병합 또는 종료 메시지를 사용한다", () => {
   const members = loadMembers(membersJson);
-  assert.match(buildPayload({ ...input, action: "closed", merged: true }, members).embeds[0].title, /머지했습니다/, "병합된 PR에는 병합 메시지를 표시해야 합니다.");
-  assert.match(buildPayload({ ...input, action: "closed", merged: false }, members).embeds[0].title, /닫았습니다/, "닫힌 PR에는 종료 메시지를 표시해야 합니다.");
+  assert.match(buildPayload({ ...input, action: "closed", merged: true }, members)?.content ?? "", /머지했습니다/, "병합된 PR에는 병합 메시지를 표시해야 합니다.");
+  assert.equal(buildPayload({ ...input, action: "closed", merged: false }, members), undefined, "병합되지 않은 종료 PR은 알림을 보내지 않아야 합니다.");
+});
+
+test("리뷰 제출 알림을 만든다", () => {
+  const payload = buildPayload({ ...input, eventName: "pull_request_review", action: "submitted", actor: "JohnPrk" }, loadMembers(membersJson));
+  assert.match(payload?.content ?? "", /티뉴님이 .*에 리뷰를 남겼습니다/, "리뷰 제출 알림에는 리뷰 작성자와 이벤트를 표시해야 합니다.");
 });
diff --git a/.github/pr-notification/discord-pr-notification.ts b/.github/pr-notification/discord-pr-notification.ts
index 7f6b8753cb30e723f23067ced2a42ceb255ed8e6..7eebd4e8df4cbfa7a33f5d75cd3010ea73c01d0b 100644
--- a/.github/pr-notification/discord-pr-notification.ts
+++ b/.github/pr-notification/discord-pr-notification.ts
@@ -13,20 +13,22 @@ export function channelForBaseBranch(baseBranch: string): string | undefined {
   return WEBHOOK_CHANNELS[baseBranch];
 }
 
-function eventFor(action: string, merged: boolean): EventDescription {
+function eventFor(eventName: string, action: string, merged: boolean): EventDescription | undefined {
+  if (eventName === "pull_request_review" && action === "submitted") {
+    return { text: "에 리뷰를 남겼습니다." };
+  }
+
   switch (action) {
     case "opened":
-      return { icon: "🆕", text: "PR을 생성했습니다" };
+      return { text: "을 생성했습니다." };
     case "synchronize":
-      return { icon: "🔄", text: "PR에 새로운 커밋을 추가했습니다" };
+      return { text: "에 새로운 커밋을 남겼습니다." };
     case "reopened":
-      return { icon: "🔁", text: "PR을 다시 열었습니다" };
+      return { text: "을 다시 열었습니다." };
     case "closed":
-      return merged
-        ? { icon: "✅", text: "PR을 머지했습니다" }
-        : { icon: "❌", text: "PR을 닫았습니다" };
+      return merged ? { text: "을 머지했습니다." } : undefined;
     default:
-      return { icon: "ℹ️", text: "PR 상태를 변경했습니다" };
+      return undefined;
   }
 }
 
@@ -34,40 +36,17 @@ function nicknameFor(login: string, members: Member[]): string {
   return members.find((member) => member.github === login)?.nickname ?? login;
 }
 
-function labelNames(labels: Label[]): string {
-  return labels
-    .map((label) => label.name)
-    .filter((name): name is string => Boolean(name))
-    .join(", ")
-    .slice(0, 1000);
-}
-
-function baseFields(input: NotificationInput, members: Member[]): DiscordField[] {
-  const author = `${nicknameFor(input.author, members)} (@${input.author})`;
-  const branch = `${input.head} → ${input.base}`;
-
-  return [
-    { name: "작성자", value: `\`${author}\``, inline: true },
-    { name: "브랜치", value: `\`${branch}\``, inline: true },
-  ];
+function typeLabel(labels: Label[]): string {
+  const label = labels.find((item) => item.name?.startsWith("type:"))?.name;
+  return label?.slice("type:".length).trim() || "none";
 }
 
-export function buildPayload(input: NotificationInput, members: Member[]): DiscordPayload {
-  const event = eventFor(input.action, input.merged);
-  const fields = baseFields(input, members);
-  const labels = labelNames(input.labels);
-
-  if (labels) fields.push({ name: "라벨", value: labels, inline: false });
-
+export function buildPayload(input: NotificationInput, members: Member[]): DiscordPayload | undefined {
+  const event = eventFor(input.eventName, input.action, input.merged);
+  if (!event) return undefined;
+  const actor = nicknameFor(input.actor, members);
   return {
-    embeds: [
-      {
-        title: `${event.icon} ${nicknameFor(input.actor, members)}님이 ${event.text}`,
-        url: input.url,
-        description: `#${input.number} ${input.title}`,
-        fields,
-      },
-    ],
+    content: `${actor}님이 [#${input.number} : ${input.title}](${input.url})${event.text}\ntype: ${typeLabel(input.labels)}`,
   };
 }
 
@@ -85,6 +64,7 @@ function labelsFromEnvironment(value: string | undefined): Label[] {
 
 function inputFromEnvironment(environment: NodeJS.ProcessEnv): NotificationInput {
   return {
+    eventName: environment.EVENT_NAME ?? "pull_request",
     action: environment.ACTION ?? "",
     merged: environment.MERGED === "true",
     number: environment.NUMBER ?? "",
@@ -98,7 +78,8 @@ function inputFromEnvironment(environment: NodeJS.ProcessEnv): NotificationInput
   };
 }
 
-export async function sendPayload(webhook: string, payload: DiscordPayload): Promise<void> {
+export async function sendPayload(webhook: string, payload: DiscordPayload | undefined): Promise<void> {
+  if (!payload) return;
   let lastError: Error | undefined;
 
   for (let attempt = 0; attempt <= RETRY_COUNT; attempt += 1) {
@@ -150,26 +131,15 @@ type Label = {
 };
 
 type EventDescription = {
-  icon: string;
   text: string;
 };
 
-type DiscordField = {
-  name: string;
-  value: string;
-  inline: boolean;
-};
-
 type DiscordPayload = {
-  embeds: Array<{
-    title: string;
-    url: string;
-    description: string;
-    fields: DiscordField[];
-  }>;
+  content: string;
 };
 
 export type NotificationInput = {
+  eventName: string;
   action: string;
   merged: boolean;
   number: string;
diff --git a/.github/workflows/discord-pr-notification.yml b/.github/workflows/discord-pr-notification.yml
index 1c0eea5b62079dc99452d47b3f58e450d0afdbe3..fbb08cfee106ea49c84f152c2accc21f7b025c77 100644
--- a/.github/workflows/discord-pr-notification.yml
+++ b/.github/workflows/discord-pr-notification.yml
@@ -7,11 +7,13 @@ on:
       - synchronize
       - reopened
       - closed
+  pull_request_review:
+    types:
+      - submitted
 
 permissions:
   contents: read
   pull-requests: write
-  pull-requests: write
 
 concurrency:
   group: discord-pr-${{ github.event.pull_request.number }}
@@ -47,6 +49,7 @@ jobs:
         shell: bash
         env:
           ACTION: ${{ github.event.action }}
+          EVENT_NAME: ${{ github.event_name }}
           MERGED: ${{ github.event.pull_request.merged }}
           NUMBER: ${{ github.event.pull_request.number }}
           TITLE: ${{ github.event.pull_request.title }}
