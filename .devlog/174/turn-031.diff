diff --git a/.github/pr-notification/discord-pr-notification.test.ts b/.github/pr-notification/discord-pr-notification.test.ts
index 93900f1e5bc041ba0c8712b3a604792c74a13c42..7d8b675641707f0b785aed262f55bfca859dbb07 100644
--- a/.github/pr-notification/discord-pr-notification.test.ts
+++ b/.github/pr-notification/discord-pr-notification.test.ts
@@ -1,7 +1,8 @@
 import assert from "node:assert/strict";
 import test from "node:test";
 
-import { buildPayload, channelForBaseBranch, loadMembers } from "./discord-pr-notification.ts";
+import { loadMembers } from "./member-configuration.ts";
+import { buildPayload, channelForBaseBranch } from "./discord-pr-notification.ts";
 
 const input = { action: "opened", merged: false, number: "174", title: "코드 리뷰를 빠르게 하기", url: "https://github.com/woowacourse-teams/2026-bibbidi/pull/174", head: "feature/174", base: "release-fe", actor: "kdongsu5509", author: "boyekim", labels: [{ name: "type: feature" }, { name: "area: fe" }] };
 const membersJson = JSON.stringify({ members: [
diff --git a/.github/pr-notification/discord-pr-notification.ts b/.github/pr-notification/discord-pr-notification.ts
index 5afa5e76b82d3d5f995d2e1a29407d575cc2469d..40b73b6ba7131e92e054d894c7a78d8d229eb7c0 100644
--- a/.github/pr-notification/discord-pr-notification.ts
+++ b/.github/pr-notification/discord-pr-notification.ts
@@ -1,3 +1,5 @@
+import { loadMembers, type Member } from "./member-configuration.ts";
+
 const WEBHOOK_CHANNELS: Record<string, string> = {
   "release-be": "BE",
   "release-fe": "FE",
@@ -28,33 +30,6 @@ function eventFor(action: string, merged: boolean): EventDescription {
   }
 }
 
-function isMember(value: unknown): value is Member {
-  if (!value || typeof value !== "object") return false;
-  if (!("github" in value) || !("nickname" in value)) return false;
-  if (typeof value.github !== "string" || typeof value.nickname !== "string") return false;
-  return !("discordUserId" in value) || typeof value.discordUserId === "string";
-}
-
-export function loadMembers(value: string | undefined): Member[] {
-  if (!value) throw new Error("PR_NOTIFICATION_MEMBERS_JSON 시크릿이 필요합니다.");
-
-  let parsed: unknown;
-  try {
-    parsed = JSON.parse(value);
-  } catch {
-    throw new Error("PR_NOTIFICATION_MEMBERS_JSON은 올바른 JSON 형식이어야 합니다.");
-  }
-
-  if (!parsed || typeof parsed !== "object" || !("members" in parsed) || !Array.isArray(parsed.members)) {
-    throw new Error("PR_NOTIFICATION_MEMBERS_JSON은 members 배열을 포함해야 합니다.");
-  }
-  if (!parsed.members.every(isMember)) {
-    throw new Error("각 구성원은 문자열 형식의 github과 nickname을 포함해야 합니다.");
-  }
-
-  return parsed.members;
-}
-
 function nicknameFor(login: string, members: Member[]): string {
   return members.find((member) => member.github === login)?.nickname ?? login;
 }
@@ -151,7 +126,6 @@ export async function sendPayload(webhook: string, payload: DiscordPayload): Pro
 export async function main(environment = process.env): Promise<void> {
   const input = inputFromEnvironment(environment);
   const channel = channelForBaseBranch(input.base);
-
   if (!channel) return;
 
   const webhook = environment[`DISCORD_WEBHOOK_${channel}`];
@@ -195,12 +169,6 @@ type DiscordPayload = {
   }>;
 };
 
-export type Member = {
-  github: string;
-  nickname: string;
-  discordUserId?: string;
-};
-
 export type NotificationInput = {
   action: string;
   merged: boolean;
diff --git a/.github/pr-notification/member-configuration.ts b/.github/pr-notification/member-configuration.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b0724e739082d2d523fde4677a3187ebc79e801d
--- /dev/null
+++ b/.github/pr-notification/member-configuration.ts
@@ -0,0 +1,34 @@
+export type Member = {
+  github: string;
+  nickname: string;
+  discordUserId?: string;
+  reviewer?: boolean;
+};
+
+function isMember(value: unknown): value is Member {
+  if (!value || typeof value !== "object") return false;
+  if (!("github" in value) || !("nickname" in value)) return false;
+  if (typeof value.github !== "string" || typeof value.nickname !== "string") return false;
+  if ("discordUserId" in value && typeof value.discordUserId !== "string") return false;
+  return !("reviewer" in value) || typeof value.reviewer === "boolean";
+}
+
+export function loadMembers(value: string | undefined): Member[] {
+  if (!value) throw new Error("PR_NOTIFICATION_MEMBERS_JSON 시크릿이 필요합니다.");
+
+  let parsed: unknown;
+  try {
+    parsed = JSON.parse(value);
+  } catch {
+    throw new Error("PR_NOTIFICATION_MEMBERS_JSON은 올바른 JSON 형식이어야 합니다.");
+  }
+
+  if (!parsed || typeof parsed !== "object" || !("members" in parsed) || !Array.isArray(parsed.members)) {
+    throw new Error("PR_NOTIFICATION_MEMBERS_JSON은 members 배열을 포함해야 합니다.");
+  }
+  if (!parsed.members.every(isMember)) {
+    throw new Error("각 구성원은 문자열 형식의 github과 nickname을 포함해야 합니다.");
+  }
+
+  return parsed.members;
+}
diff --git a/.github/pr-notification/reviewer-assignment.test.ts b/.github/pr-notification/reviewer-assignment.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9a8bb7d5545b004538522aba903d3184c9cfdd5d
--- /dev/null
+++ b/.github/pr-notification/reviewer-assignment.test.ts
@@ -0,0 +1,21 @@
+import assert from "node:assert/strict";
+import test from "node:test";
+
+import { selectReviewer } from "./reviewer-assignment.ts";
+
+const candidates = [
+  { github: "kdongsu5509" },
+  { github: "byunghyunkim0", reviewer: false },
+  { github: "boyekim" },
+  { github: "JohnPrk" },
+];
+
+test("assigns reviewers by PR-number round robin while skipping the author and excluded members", () => {
+  assert.equal(selectReviewer({ candidates, author: "kdongsu5509", pullNumber: 1, existingReviewers: [] }), "boyekim");
+  assert.equal(selectReviewer({ candidates, author: "boyekim", pullNumber: 3, existingReviewers: [] }), "JohnPrk");
+  assert.equal(selectReviewer({ candidates, author: "JohnPrk", pullNumber: 4, existingReviewers: [] }), "kdongsu5509");
+});
+
+test("does not replace a reviewer already selected for the PR", () => {
+  assert.equal(selectReviewer({ candidates, author: "kdongsu5509", pullNumber: 1, existingReviewers: ["boyekim"] }), undefined);
+});
diff --git a/.github/pr-notification/reviewer-assignment.ts b/.github/pr-notification/reviewer-assignment.ts
new file mode 100644
index 0000000000000000000000000000000000000000..d86bfadb930b6cf754af718be163802c97833d5f
--- /dev/null
+++ b/.github/pr-notification/reviewer-assignment.ts
@@ -0,0 +1,102 @@
+export type ReviewerCandidate = {
+  github: string;
+  reviewer?: boolean;
+};
+
+type ReviewerAssignmentInput = {
+  candidates: ReviewerCandidate[];
+  author: string;
+  pullNumber: number;
+  existingReviewers: string[];
+};
+
+type ReviewerRequest = {
+  repository: string;
+  pullNumber: string;
+  reviewer: string;
+  token: string;
+};
+
+export function selectReviewer({
+  candidates,
+  author,
+  pullNumber,
+  existingReviewers,
+}: ReviewerAssignmentInput): string | undefined {
+  if (existingReviewers.length > 0) return undefined;
+
+  const availableCandidates = candidates.filter((candidate) => candidate.reviewer !== false);
+  if (availableCandidates.length === 0) return undefined;
+
+  const startIndex = Math.max(pullNumber - 1, 0) % availableCandidates.length;
+  for (let offset = 0; offset < availableCandidates.length; offset += 1) {
+    const candidate = availableCandidates[(startIndex + offset) % availableCandidates.length];
+    if (candidate.github !== author) return candidate.github;
+  }
+
+  return undefined;
+}
+
+export function existingReviewerLogins(value: string | undefined): string[] {
+  if (!value) return [];
+
+  try {
+    const reviewers: unknown = JSON.parse(value);
+    if (!Array.isArray(reviewers)) return [];
+
+    return reviewers.flatMap((reviewer) => (
+      reviewer && typeof reviewer === "object" && "login" in reviewer && typeof reviewer.login === "string"
+        ? [reviewer.login]
+        : []
+    ));
+  } catch {
+    console.warn("::warning::기존 PR 리뷰어 정보를 해석하지 못했습니다.");
+    return [];
+  }
+}
+
+export async function requestReviewer({ repository, pullNumber, reviewer, token }: ReviewerRequest): Promise<void> {
+  const response = await fetch(`https://api.github.com/repos/${repository}/pulls/${pullNumber}/requested_reviewers`, {
+    method: "POST",
+    headers: {
+      Accept: "application/vnd.github+json",
+      Authorization: `Bearer ${token}`,
+      "Content-Type": "application/json",
+      "X-GitHub-Api-Version": "2022-11-28",
+    },
+    body: JSON.stringify({ reviewers: [reviewer] }),
+  });
+
+  if (!response.ok) {
+    throw new Error(`GitHub 리뷰어 지정 요청에 실패했습니다. (상태 코드: ${response.status})`);
+  }
+}
+
+export async function main(environment = process.env): Promise<void> {
+  const existingReviewers = existingReviewerLogins(environment.REQUESTED_REVIEWERS_JSON);
+  const reviewer = selectReviewer({
+    candidates: loadMembers(environment.PR_NOTIFICATION_MEMBERS_JSON),
+    author: environment.AUTHOR ?? "",
+    pullNumber: Number(environment.NUMBER),
+    existingReviewers,
+  });
+
+  if (!reviewer) {
+    if (existingReviewers.length === 0) console.warn("::warning::자동 배정할 리뷰어 후보가 없습니다.");
+    return;
+  }
+
+  const repository = environment.GITHUB_REPOSITORY;
+  const token = environment.GITHUB_TOKEN;
+  if (!repository || !token) throw new Error("GitHub 리뷰어 지정을 위한 저장소 정보 또는 토큰이 없습니다.");
+
+  await requestReviewer({ repository, pullNumber: environment.NUMBER ?? "", reviewer, token });
+}
+
+if (process.argv[1] && import.meta.url === new URL(`file://${process.argv[1]}`).href) {
+  main().catch((error: unknown) => {
+    console.error(error);
+    process.exitCode = 1;
+  });
+}
+import { loadMembers } from "./member-configuration.ts";
diff --git a/.github/workflows/discord-pr-notification.yml b/.github/workflows/discord-pr-notification.yml
index b9d3aad6c26c02124434a61a7e351afddf479596..1c0eea5b62079dc99452d47b3f58e450d0afdbe3 100644
--- a/.github/workflows/discord-pr-notification.yml
+++ b/.github/workflows/discord-pr-notification.yml
@@ -10,6 +10,8 @@ on:
 
 permissions:
   contents: read
+  pull-requests: write
+  pull-requests: write
 
 concurrency:
   group: discord-pr-${{ github.event.pull_request.number }}
@@ -29,6 +31,18 @@ jobs:
         with:
           node-version: 22.18.0
 
+      - name: Assign reviewer
+        if: github.event.action == 'opened'
+        shell: bash
+        env:
+          AUTHOR: ${{ github.event.pull_request.user.login }}
+          NUMBER: ${{ github.event.pull_request.number }}
+          REQUESTED_REVIEWERS_JSON: ${{ toJson(github.event.pull_request.requested_reviewers) }}
+          GITHUB_REPOSITORY: ${{ github.repository }}
+          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
+          PR_NOTIFICATION_MEMBERS_JSON: ${{ secrets.PR_NOTIFICATION_MEMBERS_JSON }}
+        run: node --experimental-strip-types .github/pr-notification/reviewer-assignment.ts
+
       - name: Send Discord notification
         shell: bash
         env:
@@ -42,6 +56,8 @@ jobs:
           ACTOR: ${{ github.actor }}
           AUTHOR: ${{ github.event.pull_request.user.login }}
           LABELS_JSON: ${{ toJson(github.event.pull_request.labels) }}
+          REQUESTED_REVIEWERS_JSON: ${{ toJson(github.event.pull_request.requested_reviewers) }}
+          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
           DISCORD_WEBHOOK_BE: ${{ secrets.DISCORD_WEBHOOK_BE }}
           DISCORD_WEBHOOK_FE: ${{ secrets.DISCORD_WEBHOOK_FE }}
           DISCORD_WEBHOOK_APP: ${{ secrets.DISCORD_WEBHOOK_APP }}
