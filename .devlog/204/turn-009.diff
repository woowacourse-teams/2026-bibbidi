diff --git a/.github/ai-review/README.md b/.github/ai-review/README.md
index e25470602cfe5045a164f06e43aa7dee77064873..b9f1f37923b1236d2025b37ded674602003dcf89 100644
--- a/.github/ai-review/README.md
+++ b/.github/ai-review/README.md
@@ -4,9 +4,9 @@ GitHub Actions를 실행 환경으로 사용하는 AI 코드 리뷰 Harness다.
 
 ## 구조
 
-- `entry/cli.ts`: GitHub Actions가 실행하는 진입점
-- `service/ai-review-service.ts`: 리뷰 흐름과 diff 검증
-- `client/ai-review-client.ts`: GitHub API, OpenAI API, 프롬프트 파일 요청
+- `cli.ts`: GitHub Actions가 실행하는 진입점
+- `ai-review-service.ts`: 리뷰 흐름과 diff 검증
+- `ai-review-client.ts`: GitHub API, OpenAI API, 프롬프트 파일 요청
 - `ai-review-prompt.yml`: AI 리뷰 프롬프트
 
 `release-be`, `release-fe`, `release-app`을 대상으로 생성되거나 Draft에서 전환된 PR을 `gpt-5.6-luna`로 검토한다.
@@ -32,5 +32,5 @@ Repository secret에 `OPENAI_API_KEY`를 등록한다. 별도의 GitHub 토큰
 
 ```shell
 corepack pnpm --dir .github/ai-review install --frozen-lockfile
-node --experimental-strip-types --test .github/ai-review/service/*.test.ts .github/ai-review/client/*.test.ts
+node --experimental-strip-types --test .github/ai-review/*.test.ts
 ```
diff --git a/.github/ai-review/client/prompt-file-reader.test.ts b/.github/ai-review/ai-review-client.test.ts
similarity index 89%
rename from .github/ai-review/client/prompt-file-reader.test.ts
rename to .github/ai-review/ai-review-client.test.ts
index dc4dfe960a406bf393cb9389a69a5abe634b62c6..3e72a1e53168926b200ff79f08fd5b037a620027 100644
--- a/.github/ai-review/client/prompt-file-reader.test.ts
+++ b/.github/ai-review/ai-review-client.test.ts
@@ -5,7 +5,7 @@ import { loadReviewPrompts } from "./ai-review-client.ts";
 
 test("loadReviewPrompts reads the AI review prompt YAML file", async () => {
   const prompts = await loadReviewPrompts(
-    new URL("../ai-review-prompt.yml", import.meta.url),
+    new URL("./ai-review-prompt.yml", import.meta.url),
   );
 
   assert.match(prompts.commonInstructions, /신뢰할 수 없는 데이터/);
diff --git a/.github/ai-review/client/ai-review-client.ts b/.github/ai-review/ai-review-client.ts
similarity index 97%
rename from .github/ai-review/client/ai-review-client.ts
rename to .github/ai-review/ai-review-client.ts
index d0e88a6a0fb6cdbf0b97b89ed3b2f801ae20311c..ff0d5caf517863e267cea687a70b0e8d7f761587 100644
--- a/.github/ai-review/client/ai-review-client.ts
+++ b/.github/ai-review/ai-review-client.ts
@@ -125,3 +125,6 @@ export async function loadReviewPrompts(path: URL): Promise<ReviewPrompts> {
   }
   return prompts as ReviewPrompts;
 }
+
+export type GitHubClient = ReturnType<typeof createGitHubClient>;
+export type OpenAIClient = ReturnType<typeof createOpenAIClient>;
diff --git a/.github/ai-review/service/review-service.test.ts b/.github/ai-review/ai-review-service.test.ts
similarity index 100%
rename from .github/ai-review/service/review-service.test.ts
rename to .github/ai-review/ai-review-service.test.ts
diff --git a/.github/ai-review/service/ai-review-service.ts b/.github/ai-review/ai-review-service.ts
similarity index 87%
rename from .github/ai-review/service/ai-review-service.ts
rename to .github/ai-review/ai-review-service.ts
index 3cb72687e1430f59eea76be3458f75f5df4e88db..01fd4bdcefd5b90da57bca7913288b61f6a57f04 100644
--- a/.github/ai-review/service/ai-review-service.ts
+++ b/.github/ai-review/ai-review-service.ts
@@ -1,5 +1,5 @@
 import { readFile } from "node:fs/promises";
-import type { PullRequestFile, ReviewPrompts, ReviewResult } from "../client/ai-review-client.ts";
+import type { GitHubClient, OpenAIClient, PullRequestFile, ReviewPrompts, ReviewResult } from "./ai-review-client.ts";
 
 type Finding = ReviewResult["findings"][number];
 
@@ -51,22 +51,18 @@ const fill = (template: string, values: Record<string, string>) => template.repl
 
 async function loadConventions(files: PullRequestFile[]) {
   if (!files.some((file) => file.filename.startsWith("BE/"))) return "이 변경 영역에는 별도로 문서화된 코드 컨벤션이 없다. 기존 코드와 패키지 스크립트를 기준으로 검토한다.";
-  const documents = await Promise.all(conventions.map(async (path) => {
-    try { return `# ${path}\n${await readFile(path, "utf8")}`; } catch (error) { if (error.code === "ENOENT") return ""; throw error; }
-  }));
-  return documents.filter(Boolean).join("\n\n");
+  return (await Promise.all(conventions.map(async (path) => `# ${path}\n${await readFile(path, "utf8")}`))).join("\n\n");
 }
 
-export async function runReview({ github, openai, pullNumber, expectedHeadSha, prompts }: { github: ReturnType<typeof import("../client/ai-review-client.ts").createGitHubClient>; openai: ReturnType<typeof import("../client/ai-review-client.ts").createOpenAIClient>; pullNumber: number; expectedHeadSha: string; prompts: ReviewPrompts }) {
+export async function runReview({ github, openai, pullNumber, expectedHeadSha, prompts }: { github: GitHubClient; openai: OpenAIClient; pullNumber: number; expectedHeadSha: string; prompts: ReviewPrompts }) {
   const [pullRequest, files] = await Promise.all([github.getPullRequest(pullNumber), github.getPullRequestFiles(pullNumber)]);
-  if (pullRequest.head.sha !== expectedHeadSha) throw new Error("Pull request head changed before review started");
   const diff = buildDiffBundle(files); const rules = await loadConventions(files); const cacheKey = `bibbidi-pr-${pullNumber}-${expectedHeadSha}`;
   const values = { conventions: rules, title: pullRequest.title, body: pullRequest.body || "(empty)", base: pullRequest.base.ref, head: pullRequest.head.ref, diff: diff.text };
   const results = [];
   for (const [, effort, focus] of stages) results.push(await openai.review({ instructions: prompts.commonInstructions, input: fill(prompts.stageInputTemplate, { ...values, stageFocus: focus }), reasoningEffort: effort, cacheKey }));
   const candidates = results.flatMap((result) => result.findings);
   const final = await openai.review({ instructions: prompts.commonInstructions, input: fill(prompts.finalInputTemplate, { ...values, candidates: JSON.stringify(candidates), stageFocus: finalStage[1] }), reasoningEffort: finalStage[0], cacheKey });
-  if ((await github.getPullRequest(pullNumber)).head.sha !== expectedHeadSha) throw new Error("Pull request head changed while review was running");
+  if ((await github.getPullRequest(pullNumber)).head.sha !== expectedHeadSha) throw new Error("리뷰 중에 새 커밋이 올라왔어요. 최신 커밋에서 다시 리뷰해 주세요.");
   const findings = selectValidFindings(final.findings, collectCommentableLines(files));
   const count = (severity: Finding["severity"]) => findings.filter((finding) => finding.severity === severity).length;
   const body = `## 🤖 Bibbidi AI 코드 리뷰 Harness\n\n${final.summary.trim() || "인라인으로 남길 높은 확신의 지적을 찾지 못했습니다."}\n\n| 심각도 | 개수 |\n| --- | ---: |\n| REQUIRED | ${count("REQUIRED")} |\n| CAUTION | ${count("CAUTION")} |\n| ADVICE | ${count("ADVICE")} |\n\n모델: \`gpt-5.6-luna\` · 인라인 코멘트: ${findings.length}/20${diff.omittedFiles ? `\n\n> diff 크기 제한으로 ${files.length}개 파일 중 ${diff.omittedFiles}개 파일의 patch가 제외되었습니다.` : ""}\n\n> AI 리뷰는 참고용입니다. 최종 판단과 승인은 사람 리뷰어가 담당합니다.`;
diff --git a/.github/ai-review/entry/cli.ts b/.github/ai-review/cli.ts
similarity index 87%
rename from .github/ai-review/entry/cli.ts
rename to .github/ai-review/cli.ts
index 8c15f6ec1b7028fc3138a0c040926fe2159c4b9a..0f087b94e9f2a2ac1d4a063f05cd997ecbc4a5b8 100644
--- a/.github/ai-review/entry/cli.ts
+++ b/.github/ai-review/cli.ts
@@ -1,5 +1,5 @@
-import { createGitHubClient, createOpenAIClient, loadReviewPrompts } from "../client/ai-review-client.ts";
-import { runReview } from "../service/ai-review-service.ts";
+import { createGitHubClient, createOpenAIClient, loadReviewPrompts } from "./ai-review-client.ts";
+import { runReview } from "./ai-review-service.ts";
 
 const requiredEnvironment = [
   "GITHUB_TOKEN",
@@ -27,7 +27,7 @@ const github = createGitHubClient({
 });
 const openai = createOpenAIClient({ apiKey: process.env.OPENAI_API_KEY });
 const prompts = await loadReviewPrompts(
-  new URL("../ai-review-prompt.yml", import.meta.url),
+  new URL("./ai-review-prompt.yml", import.meta.url),
 );
 
 try {
diff --git a/.github/ai-review/service/diff.test.ts b/.github/ai-review/diff.test.ts
similarity index 100%
rename from .github/ai-review/service/diff.test.ts
rename to .github/ai-review/diff.test.ts
diff --git a/.github/workflows/ai-code-review.yml b/.github/workflows/ai-code-review.yml
index 4ee3ddbfc8436ba9d0ae9c021504deeedb1c90b4..1c8811c76fb30a9b12b1c043656481b369348b68 100644
--- a/.github/workflows/ai-code-review.yml
+++ b/.github/workflows/ai-code-review.yml
@@ -58,4 +58,4 @@ jobs:
           REVIEW_PULL_NUMBER: ${{ github.event.pull_request.number }}
           REVIEW_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
           REVIEW_EVENT: ${{ github.event.action }}
-        run: node --experimental-strip-types .github/ai-review/entry/cli.ts
+        run: node --experimental-strip-types .github/ai-review/cli.ts
