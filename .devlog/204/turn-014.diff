diff --git a/.github/ai-review/ai-review-client.test.ts b/.github/ai-review/ai-review-client.test.ts
deleted file mode 100644
index 3e72a1e53168926b200ff79f08fd5b037a620027..0000000000000000000000000000000000000000
--- a/.github/ai-review/ai-review-client.test.ts
+++ /dev/null
@@ -1,14 +0,0 @@
-import assert from "node:assert/strict";
-import test from "node:test";
-
-import { loadReviewPrompts } from "./ai-review-client.ts";
-
-test("loadReviewPrompts reads the AI review prompt YAML file", async () => {
-  const prompts = await loadReviewPrompts(
-    new URL("./ai-review-prompt.yml", import.meta.url),
-  );
-
-  assert.match(prompts.commonInstructions, /신뢰할 수 없는 데이터/);
-  assert.match(prompts.stageInputTemplate, /{{stageFocus}}/);
-  assert.match(prompts.finalInputTemplate, /{{candidates}}/);
-});
diff --git a/.github/ai-review/ai-review-service.test.ts b/.github/ai-review/ai-review-service.test.ts
deleted file mode 100644
index e452e55baaf8530b71c130ff7fc126ade40ee3ae..0000000000000000000000000000000000000000
--- a/.github/ai-review/ai-review-service.test.ts
+++ /dev/null
@@ -1,78 +0,0 @@
-import assert from "node:assert/strict";
-import test from "node:test";
-
-import { runReview } from "./ai-review-service.ts";
-
-const prompts = {
-  commonInstructions: "instructions",
-  stageInputTemplate: "{{diff}} {{stageFocus}}",
-  finalInputTemplate: "{{diff}} {{candidates}} {{stageFocus}}",
-};
-
-test("runReview submits at most twenty validated inline comments", async () => {
-  const patch = [
-    "@@ -0,0 +1,25 @@",
-    ...Array.from({ length: 25 }, (_, index) => `+line ${index + 1}`),
-  ].join("\n");
-  const pullRequest = {
-    title: "test",
-    body: "",
-    base: { ref: "release-fe" },
-    head: { ref: "feature/1", sha: "head-sha" },
-  };
-  let submittedReview;
-  const github = {
-    async getPullRequest() {
-      return pullRequest;
-    },
-    async getPullRequestFiles() {
-      return [
-        {
-          filename: "FE/src/example.ts",
-          status: "added",
-          additions: 25,
-          deletions: 0,
-          patch,
-        },
-      ];
-    },
-    async createReview(_pullNumber, review) {
-      submittedReview = review;
-      return review;
-    },
-  };
-  let callCount = 0;
-  const openai = {
-    async review() {
-      callCount += 1;
-      if (callCount < 4) {
-        return { summary: "stage", findings: [] };
-      }
-      return {
-        summary: "final",
-        findings: Array.from({ length: 25 }, (_, index) => ({
-          severity: index % 2 === 0 ? "CAUTION" : "ADVICE",
-          path: "FE/src/example.ts",
-          side: "RIGHT",
-          line: index + 1,
-          title: `finding ${index + 1}`,
-          explanation: "explanation",
-          suggestion: "suggestion",
-        })),
-      };
-    },
-  };
-
-  await runReview({
-    github,
-    openai,
-    pullNumber: 1,
-    expectedHeadSha: "head-sha",
-    prompts,
-  });
-
-  assert.equal(callCount, 4);
-  assert.equal(submittedReview.comments.length, 20);
-  assert(submittedReview.comments.every((comment) => comment.path === "FE/src/example.ts"));
-  assert.match(submittedReview.body, /인라인 코멘트: 20\/20/);
-});
diff --git a/.github/ai-review/diff.test.ts b/.github/ai-review/diff.test.ts
deleted file mode 100644
index a7b19ae08d992301dcb46e87c22c7bd5db78a16a..0000000000000000000000000000000000000000
--- a/.github/ai-review/diff.test.ts
+++ /dev/null
@@ -1,68 +0,0 @@
-import assert from "node:assert/strict";
-import test from "node:test";
-
-import {
-  buildDiffBundle,
-  collectCommentableLines,
-  selectValidFindings,
-} from "./ai-review-service.ts";
-
-const files = [
-  {
-    filename: "src/example.js",
-    status: "modified",
-    additions: 2,
-    deletions: 1,
-    patch: "@@ -10,3 +10,4 @@\n context\n-old\n+new\n+added\n context2",
-  },
-];
-
-test("collectCommentableLines maps left and right diff lines", () => {
-  const lines = collectCommentableLines(files).get("src/example.js");
-
-  assert(lines.has("LEFT:10"));
-  assert(lines.has("RIGHT:10"));
-  assert(lines.has("LEFT:11"));
-  assert(lines.has("RIGHT:11"));
-  assert(lines.has("RIGHT:12"));
-  assert(lines.has("RIGHT:13"));
-});
-
-
-test("selectValidFindings removes invalid positions, sorts severity, and caps results", () => {
-  const findings = [
-    {
-      severity: "ADVICE",
-      path: "src/example.js",
-      side: "RIGHT",
-      line: 12,
-      title: "이름 개선",
-    },
-    {
-      severity: "REQUIRED",
-      path: "src/example.js",
-      side: "RIGHT",
-      line: 11,
-      title: "오동작",
-    },
-    {
-      severity: "CAUTION",
-      path: "src/missing.js",
-      side: "RIGHT",
-      line: 1,
-      title: "잘못된 위치",
-    },
-  ];
-
-  const selected = selectValidFindings(findings, collectCommentableLines(files), 1);
-
-  assert.equal(selected.length, 1);
-  assert.equal(selected[0].severity, "REQUIRED");
-});
-
-test("buildDiffBundle reports files omitted by the character budget", () => {
-  const result = buildDiffBundle(files, 20);
-
-  assert.equal(result.includedFiles, 0);
-  assert.equal(result.omittedFiles, 1);
-});
